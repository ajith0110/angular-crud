import { DisableWeekendDirective } from './disable-weekend.directive';

describe('DisableWeekendDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableWeekendDirective();
    expect(directive).toBeTruthy();
  });
});
